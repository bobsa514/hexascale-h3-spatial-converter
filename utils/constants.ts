export const DEFAULT_H3_RESOLUTION = 8;
export const H3_RESOLUTION_MIN = 1;
export const H3_RESOLUTION_MAX = 12;

export const MAX_HEX_RENDER_COUNT = 1000;
export const VIEWPORT_HEX_RENDER_COUNT = 5000;

export const DEFAULT_MAP_CENTER: [number, number] = [37.7749, -122.4194];
export const DEFAULT_MAP_ZOOM = 10;

export const MAX_FILE_SIZE_MB = 500;
export const WARN_FILE_SIZE_MB = 100;
export const MAX_FILE_SIZE_BYTES = MAX_FILE_SIZE_MB * 1024 * 1024;
export const WARN_FILE_SIZE_BYTES = WARN_FILE_SIZE_MB * 1024 * 1024;

export const PREVIEW_ROW_LIMIT = 20;
export const RESULT_PREVIEW_ROW_LIMIT = 100;

export const PROGRESS_BATCH_SIZE = 200;

export const ONBOARDING_DISMISSED_KEY = 'hexascale_onboarding_dismissed';
